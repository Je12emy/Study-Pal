# Use .NET, SQLite, EF Core, and React PWA for v1

Study Pal v1 is a single-user personal study tracker optimized for simple hosting and quick iteration. We will use a .NET API, SQLite persistence through EF Core, and an installable React/Vite PWA; data changes require API connectivity, and offline writes, multi-user auth, and database scaling are intentionally deferred.
